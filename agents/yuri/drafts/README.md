# Yuri -- Drafts

Working documents for **Yuri** (QA / Test Writer). Test strategy, per-PR QA verdicts, sprint QA reports, HIPAA verification notes, accessibility audits, and all other QA working artifacts live here.

See the **Key Documents** table in `CLAUDE.md` for the authoritative list of current artifacts owned by Yuri.

## Conventions

- Drafts are work-in-progress — not final artifacts
- `DISPATCH-*.md` — one-time task instructions (archive after completion)
- `TASK-*.md` — iteration documents (keep latest version only)
- Final artifacts move to `outputs/` or `/artifacts/`
