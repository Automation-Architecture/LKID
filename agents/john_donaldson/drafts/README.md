# John Donaldson -- Drafts

Working documents for **John Donaldson** (API Designer / Backend Developer). API contract, prediction engine, finalized formulas, backend research, implementation notes, debug scripts, and all other backend working artifacts live here.

See the **Key Documents** table in `CLAUDE.md` for the authoritative list of current artifacts owned by John.

## Conventions

- Drafts are work-in-progress — not final artifacts
- `DISPATCH-*.md` — one-time task instructions (archive after completion)
- `TASK-*.md` — iteration documents (keep latest version only)
- Final artifacts move to `outputs/` or `/artifacts/`
