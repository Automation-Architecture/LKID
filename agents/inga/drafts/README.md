# Inga -- Drafts

Working documents for **Inga** (UX/UI Senior Designer). Wireframes, component specs, design tokens, user flows, chart specs, accessibility plans, dashboard mockups, design-sprint sign-offs, and all other UX/UI working artifacts live here.

See the **Key Documents** table in `CLAUDE.md` for the authoritative list of current artifacts owned by Inga.

## Conventions

- Drafts are work-in-progress — not final artifacts
- `DISPATCH-*.md` — one-time task instructions (archive after completion)
- `TASK-*.md` — iteration documents (keep latest version only)
- Final artifacts move to `outputs/` or `/artifacts/`
