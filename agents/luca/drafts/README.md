# Luca -- Drafts

Working documents for **Luca** (CTO / Orchestrator). Architecture, infrastructure setup, deployment checklists, medical expert review, merge plans, postmortems, sprint commit strategies, clinical Q&A with Lee, and all other orchestration working artifacts live here.

See the **Key Documents** table in `CLAUDE.md` for the authoritative list of current artifacts owned by Luca.

## Conventions

- Drafts are work-in-progress — not final artifacts
- `DISPATCH-*.md` — one-time task instructions (archive after completion)
- `TASK-*.md` — iteration documents (keep latest version only)
- Final artifacts move to `outputs/` or `/artifacts/`
